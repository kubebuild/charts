apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "kubebuild.fullname" . }}-launcher
  labels:
    chart: {{ template "kubebuild.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  {{ .Values.launcherApp.envVars }}